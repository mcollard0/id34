pluginManagement {
    repositories {
        google();
        mavenCentral();
        gradlePluginPortal();
    }
}
dependencyResolutionManagement {
    repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS;
    repositories {
        google();
        mavenCentral();
    }
}
rootProject.name = 'id34';
include ':app';
